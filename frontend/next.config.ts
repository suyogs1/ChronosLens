import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  env: {
    NEXT_PUBLIC_WS_URL: "wss://chrono-lens-backend-sknvuepfjq-uc.a.run.app"
  }
  /* config options here */
};

export default nextConfig;
